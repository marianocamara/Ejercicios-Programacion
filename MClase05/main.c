#include <stdio.h>
#include <stdlib.h>
#define CANTIDAD_DE_EMPLEADOS 3

int main()
{
    int edad [CANTIDAD_DE_EMPLEADOS;
    float sueldo [CANTIDAD_DE_EMPLEADOS;
    int indice;
    int auxEdad;
    int auxSueldo;

    for (indice=0; indice < CANTIDAD_DE_EMPLEADOS; indice++)
    {
        printf("\nIngrese la edad de la persona nº %d", indice+1);
        scanf ("%d",&auxEdad);
        edad [indice] = auxEdad;
        printf("\nIngrese la edad de la persona nº %d", indice+1);
        sueldo [indice] = auxSueldo;
    }

    for (indice=0; indice < CANTIDAD_DE_EMPLEADOS; indice++)
    {
        printf("La edad de la persona nº%d es %d", indice+1,)
    }


}
